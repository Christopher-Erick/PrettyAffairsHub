# Maintenance Guide

- Keep dependencies updated (`pip list --outdated`)
- Rotate `SECRET_KEY` and DB credentials on a schedule
- Back up PostgreSQL regularly in production
- Monitor disk for `media/` growth
- Review Django security release notes on upgrades
