# Admin Guide

Django Admin is available at `/admin/` after creating a superuser:

```bash
python manage.py createsuperuser
```

Catalog, orders, CMS, and promotions admin customizations arrive with later phases.
